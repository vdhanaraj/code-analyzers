{
  "name": "code-analyzers-root",
  "version": "0.0.0",
  "private": true,
  "description": "Repo-root orchestration: git hooks (Lefthook) and per-root pass-throughs. App packages live under ts/ (see CONVENTIONS.md).",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "postinstall": "pnpm -C ts install",
    "prepare": "lefthook install",
    "build": "pnpm -C ts build",
    "typecheck": "pnpm -C ts typecheck",
    "test": "pnpm -C ts test",
    "coverage": "pnpm -C ts exec vitest run --coverage",
    "lint": "pnpm -C ts lint",
    "format": "pnpm -C ts format",
    "analyze:self": "pnpm build && pnpm coverage && node ts/cli/dist/index.js . --coverage-report _local/tmp/coverage/coverage-final.json --lint-cwd \"$PWD/ts\" --lint-bin \"$PWD/ts/node_modules/.bin/biome\"",
    "clean": "rm -rf ts/core/dist ts/lib/dist ts/cli/dist _local/tmp"
  },
  "devDependencies": {
    "lefthook": "^1.10.0"
  }
}
